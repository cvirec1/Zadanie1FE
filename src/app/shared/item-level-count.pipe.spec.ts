import { ItemLevelCountPipe } from './item-level-count.pipe';

describe('ItemLevelCountPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemLevelCountPipe();
    expect(pipe).toBeTruthy();
  });
});
