import { ItemDatePipePipe } from './item-date-pipe.pipe';

describe('ItemDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ItemDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
