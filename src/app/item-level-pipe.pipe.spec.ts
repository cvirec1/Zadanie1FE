import { ItemLevelPipePipe } from './item-level-pipe.pipe';

describe('ItemLevelPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ItemLevelPipePipe();
    expect(pipe).toBeTruthy();
  });
});
